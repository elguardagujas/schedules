name: Build & Release

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TZ: Europe/Madrid

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install dependencies
        run: sudo apt-get install -y python3-shapely

      - name: Build
        run: |
          mkdir -p dist
          (cd tools && ./dbgen.py --station-dump ../dist/stations.json --vacuum ../dist/all.sqlite $(date -d "-1 year" +%Y-%m-%d) $(date -d "+90 days" +%Y-%m-%d) ../gtfs-static/data/)
          (cd tools && ./binconv.py --from-date $(date -d yesterday +%Y%m%d) --to-date $(date -d tomorrow +%Y%m%d) ../dist/all.sqlite ../dist/live.sched)
          (cd tools && ./binconv.py --from-date $(date -d yesterday +%Y%m%d) --to-date $(date -d "+90 days" +%Y%m%d) ../dist/all.sqlite ../dist/plan90.sched)
          (cd tools && ./binconv.py --from-date $(date -d "-1 year" +%Y%m%d) --to-date $(date +%Y%m%d) ../dist/all.sqlite ../dist/hist.sched)

      - name: Compute tag
        id: tag
        run: echo "tag=$(date -u +%Y-%m-%d_%H-%M)-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Create release
        run: |
          gh release create "${{ steps.tag.outputs.tag }}" \
            --title "${{ steps.tag.outputs.tag }}" \
            dist/all.sqlite \
            dist/live.sched \
            dist/plan90.sched \
            dist/hist.sched \
            dist/stations.json
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

